simple MessageRegistry
{
    parameters:
        @class(MessageRegistry);
        @display("i=block/table");

        int firstOrder = default(1);
        int minEntities = default(1);
        int maxEntities = default(4);
}
