# Attacks for CAVISE V2X ecosystem

Each attack is described in the corresponding section of `omnetpp.ini`. Most
attacks expose strategy parameters that control their impact and how difficult
they are to detect.

Under `vis/`, there are scripts for making nice visualizations. You may use them as a reference,
but note that outputs from OpenCDA may change over time and those helpers may become invalid.
